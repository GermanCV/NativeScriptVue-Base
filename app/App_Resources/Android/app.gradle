// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "german.app.es"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
